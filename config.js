/* ═══════════════════════════════════════════════════════════════════
   ASNM — 클라이언트 설정
   ───────────────────────────────────────────────────────────────────
   이 파일은 git에 커밋해도 안전합니다. 비밀 값이 없습니다.

   API 키는 절대 여기에 넣지 마세요.
   GitHub Pages는 정적 호스팅이므로, 여기 넣은 키는 누구나 볼 수 있고
   자동 스캐너가 몇 분 안에 찾아냅니다.

   대신 Cloudflare Worker(worker.js)를 배포하고, 그 URL을 아래에 넣습니다.
   키는 Worker의 환경변수에만 저장됩니다.

   aiProxyUrl 이 비어 있으면 사이트는 '데모 모드'로 동작합니다.
   모듈 1~3의 스코어·리스크 분석은 규칙 기반이므로 AI 없이도 완전히 작동합니다.
   ═══════════════════════════════════════════════════════════════════ */

window.ASNM_CONFIG = {

  // Cloudflare Worker 배포 후 받은 URL을 여기에 붙여넣으세요.
  // 예: 'https://asnm-ai.your-subdomain.workers.dev'
  aiProxyUrl: '',

  // 표시용 모델명 (실제 모델은 Worker에서 결정됩니다)
  modelLabel: 'GPT-4o mini',

  // 사이트 단계 표기 — 페이지 여러 곳에서 참조
  stage: 'Early Research Prototype',

  // 문의 이메일 (푸터·연락 CTA에서 사용)
  contactEmail: ''
};
